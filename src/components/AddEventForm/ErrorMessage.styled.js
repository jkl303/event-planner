import styled from "styled-components";

export const ErrorMessageStyled = styled.p`
  position: absolute;
  width: 100%;
  right: 19px;
  margin-top: 4px;

  text-align: right;
  font-size: 12px;
  line-height: 1.33;
  color: var(--high);
`;
